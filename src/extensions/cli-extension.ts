
import { GluegunToolbox } from 'gluegun'

  
module.exports = (toolbox: GluegunToolbox) => {
  // enable this if you want to read configuration in from
  // the current folder's package.json (in a "clitt" property),
  // clitt.config.json, etc.
  // toolbox.config = {
  //   ...toolbox.config,
  //   ...toolbox.config.loadConfig(process.cwd(), "clitt")
  // }
}
